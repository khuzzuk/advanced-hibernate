spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/posts
    username: post
    password: post
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true